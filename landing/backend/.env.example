# MongoDB Atlas connection string (PortFolio cluster)
MONGODB_URI=mongodb+srv://prajapatipm16:PASSWORD@portfolio.noz1l.mongodb.net/hopiin-landing?retryWrites=true&w=majority&appName=PortFolio

# Server port (Vite proxies /api → this port in dev)
PORT=3000

# Comma-separated origins allowed for CORS (frontend URL in prod)
CORS_ORIGIN=http://localhost:5174
